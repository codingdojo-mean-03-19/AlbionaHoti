let x = 1;
let y = 2;
let z == 3;

console.log(x+x);

/*
    Syntax errors: happen when the parser finds invalid JavaScript.
    The moment an error is found, the parser terminates and the program
    ends without any code ever being run.
*/


/*
    Runtime Errors:
    - is what happens if your code successfully parses and the error
    happens while the program is running.
    - bugs
    - examples: faulty logic or improperly written code. 
*/